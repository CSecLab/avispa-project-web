role alice(
A,B        : agent,
K            : symmetric_key
"Hash"       : hash_func),
SND,RCV : channel(dy)
played_by A def =

local
state    : nat,
Na,Nb  : text,
K1       : message

init

stata := 0

transition

end role
  