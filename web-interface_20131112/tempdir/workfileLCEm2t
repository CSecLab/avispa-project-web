role player(A,B:agent, Send,Rcv : channel(dy))
played_by A def=
  local State: nat,
        Kdh: symmetric_key,
        x: test
  init State := 0
  transition

  step1. State = 0 =|> State' := 1 /\ x' := new(); Send(exp(g,x'))
end role 